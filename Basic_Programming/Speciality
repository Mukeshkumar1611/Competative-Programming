#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin >> t;
	while(t--)
	{
	    int a, b, c;
	    cin >> a >> b >> c;
	    int maxi = max(a, max(b,c));
	    if(maxi== a)
	    {
	        cout << "Setter" << endl;
	    }
	    else if(maxi == b)
	    {
	        cout << "Tester" << endl;
	    }
	    else{
	        cout << "Editorialist" << endl;
	    }
	}
	return 0;
}

